| Component | Status | Evidence | Gaps | Suggested Next Steps |
| --- | --- | --- | --- | --- |
| Local database layout (bronze/silver/gold or equivalent) | Partial | LocalDataHub only provisions flat `market/`, `fundamentals/`, `news/`, `reports/`, and `metadata/` folders with a single catalog file and no per-company tiering.【F:src/gnn_trading/data_hub.py†L69-L198】 | No company-specific bronze/silver/gold hierarchy, no version snapshots, and no manifest metadata beyond simple counts. | Extend the hub to create `database/companies/<symbol>/{bronze,silver,gold}/...` along with shared/version folders and manifest writers. |
| Timeframe-bounded ingest for prices, fundamentals, news/filings | Missing | The operational CLI exposes commands for holdings, scoring, planning, etc., but there is no `update` flow or connectors; only legacy scripts download Yahoo data in isolation.【F:src/gnn_trading/fund_manager.py†L338-L618】【F:src/gnn_trading/src/gnn_trading/data_prep_yahoo.py†L1-L49】 | No unified ingest pipeline, no SEC/GDELT connectors, no manifest hashing, and no start/end filtering in the fund manager. | Implement structured ingestors for Stooq, Yahoo fallback, SEC filings/facts, and GDELT with `--start-date/--end-date` controls and manifest outputs; wire them into an `update` CLI. |
| Leakage-safe feature builder & labels for H=5/20/50 | Partial | Feature engineering uses simple rolling calculations without explicit leakage guards, and labels only generate a single breakout target; dataset windows follow a single configurable horizon.【F:src/gnn_trading/features.py†L4-L103】【F:src/gnn_trading/labels.py†L3-L15】【F:src/gnn_trading/dataset.py†L8-L55】 | Missing horizon-specific features/labels (ret/vol/breakout for 5/20/50), no coverage reports, and no leakage validation harness. | Add multi-horizon label generation, enforce temporal cutoffs, emit coverage diagnostics, and introduce automated leakage tests. |
| Cross-sectional batching & lagged correlation edges | Missing | The dataset constructs a single-chain edge index per ticker window with no cross-asset batching or correlation graph building.【F:src/gnn_trading/dataset.py†L28-L55】 | No shared correlation graph, no multi-ticker batching, and no shared/corr_daily outputs. | Build cross-sectional loaders that stitch tickers into correlation graphs and persist lagged correlation parquet files. |
| Model registry on disk (save/load, latest symlink) | Missing | The quantamental module defines an in-memory `ModelRegistry` that simply maps names to estimator instances with no persistence or metadata export.【F:src/gnn_trading/quantamental.py†L190-L211】 | No disk-backed registry, no config/feature spec snapshots, no metrics manifests, and no `latest` symlink handling. | Implement a filesystem registry at `./models/gnn_crosssec/<run_id>` capturing weights, configs, specs, metrics, and maintain a `latest` symlink. |
| LLM extractor with timeframe, schema, provenance | Missing | The AI housekeeper only issues prompts via an optional LLM client for planning; there is no structured extraction, schema validation, or provenance capture.【F:src/gnn_trading/ai_housekeeper.py†L26-L88】 | No SEC/IR/GDELT discovery, no OpenAI tool-calling integration, no JSON schema validation, and no provenance ledger. | Build an extractor CLI that discovers sources, fetches documents, calls OpenAI for structured JSON outputs, validates against schema, and stores silver artefacts with provenance metadata. |
| Walk-forward evaluation (purged/embargoed) | Partial | Training offers a basic `TimeSeriesSplit` walk-forward loop but reuses contiguous splits without purging or embargo windows and writes a single signals CSV.【F:src/gnn_trading/train.py†L69-L115】 | Missing purging/embargo handling, horizon awareness, calibration outputs, and metrics aggregation per fold. | Introduce purged walk-forward splitting with embargo, collect per-fold metrics/calibration, and persist manifests. |
| UI (Data Manager, Feature/Label Builder, Model Lab, Fund Manager Agent, Configs, Monitoring) | Missing | The only UI surface is a Tkinter fund manager window focused on holdings and analysis tables; no web UI or dedicated pages exist.【F:src/gnn_trading/fund_manager_gui.py†L1-L506】 | Lacks modern multi-page UI (Data Manager, Feature Builder, Model Lab, Fund Agent, Configs, Monitoring) and IPC bridge. | Develop a Next.js/Tauri front-end (or similar) with required pages and REST/IPC hooks to orchestrate CLI jobs. |
| Backtest & recommendation surface | Partial | CLI/GUI fund manager plus batch recommender and a simple breakout backtester provide decisions and equity curves.【F:src/gnn_trading/fund_manager.py†L336-L618】【F:src/gnn_trading/fund_manager_recommend.py†L1-L138】【F:src/gnn_trading/backtest.py†L1-L35】 | No horizon-specific signals, no MC-dropout confidence surfacing in UI, limited rationale instrumentation, and no interactive backtest widget. | Expand reporting to include multi-horizon scores, uncertainty bands, SHAP/feature narratives, and embed a richer backtest dashboard. |
| Config management (YAML, env, secrets) | Missing | While a sample YAML exists, the runtime relies on CLI flags and does not load YAML configs; there is no secrets or environment handling beyond defaults.【F:config_example.yaml†L1-L34】【F:src/gnn_trading/train.py†L150-L176】 | No configuration loader, no environment override patterns, and no secrets management hooks. | Implement a central config loader that reads YAML, merges CLI overrides, and documents environment/secrets handling for providers. |
